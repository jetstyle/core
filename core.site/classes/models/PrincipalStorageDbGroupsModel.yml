default:
    table: users_groups
    fields:
        - id
        - title
        - god
        - custom
    order:
        _order: ASC

cms:
    table: users_groups
    fields:
        - id
        - title
        - god
        - custom
        - _state
    order:
        _order: ASC
